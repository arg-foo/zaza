Update the `tiger-brokers-cash-mcp` git submodule to the latest commit on its remote default branch.

Steps:
1. Run `git submodule update --remote tiger-brokers-cash-mcp` to pull the latest commit.
2. Run `git status` to check if the submodule reference changed.
3. If the submodule was updated, stage and commit the change with a message like: "Update tiger-brokers-cash-mcp submodule to latest"
4. Show the new submodule commit hash after updating.
